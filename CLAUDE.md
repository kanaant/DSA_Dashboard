# CLAUDE.md

For any fresh checkout, read `ONBOARDING.md` first.

If you want the alias entrypoint, `AGENT.md` points to the same onboarding flow.

The reusable onboarding workflow lives in `skills/software-development/dsa-dashboard-onboarding/SKILL.md`.

Keep the dashboard grounded in the configurable agent control-center identity:
- DarkSenses branding with the selected agent name and avatar
- real app title, not a template
- branded icons in `src/app/`
- no client-side exposure of Hermes bearer secrets
